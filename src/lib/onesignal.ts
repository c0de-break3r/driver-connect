export async function setupOneSignal() {
  // OneSignal is temporarily disabled until a compatible native build is available.
  return;
}

export async function getOneSignalPlayerId(): Promise<string | null> {
  // OneSignal is temporarily disabled until a compatible native build is available.
  return null;
}
